spring:
  config:
    import:
      - optional:classpath:api-info.yml

server:
  port: 8888

lucky:
  http-client:
    http-executor: jdk
    #日志相关的配置
    logger:
      packages:
        - io.github.lucklike.luckyclient.api

lucky-server: http://localhost:8864



